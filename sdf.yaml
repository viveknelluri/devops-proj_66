name : on_move 

on : [push,pull_request]

jobs:
   build:
      runs-on: ubuntu-latest
      steps:
          -uses: actions/checkout@v2
          -name:  "a script"
          run : echo "hdndnd"

          